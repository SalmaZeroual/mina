import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary    = Color(0xFFFA003F);
  static const primaryDark = Color(0xFFCC0032);
  static const primaryLight = Color(0xFFFF3363);

  // Neutrals
  static const black      = Color(0xFF0A0A0A);
  static const dark       = Color(0xFF111111);
  static const surface    = Color(0xFF1A1A1A);
  static const surface2   = Color(0xFF242424);
  static const grey       = Color(0xFF3A3A3A);
  static const greyLight  = Color(0xFF6A6A6A);
  static const greyMuted  = Color(0xFF9A9A9A);
  static const white      = Color(0xFFFFFFFF);
  static const offWhite   = Color(0xFFF5F5F5);
  static const border     = Color(0xFF2A2A2A);

  // States
  static const success    = Color(0xFF22C55E);
  static const warning    = Color(0xFFF59E0B);
  static const error      = Color(0xFFEF4444);
}